{
  "name": "expenzetracker-api",
  "version": "1.0.0",
  "description": "api for expenze tracker",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "node server.js && cd ExpenzeTracker-Client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^6.4.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "mongoose": "^5.6.9",
    "nodemailer": "^6.3.0",
    "passport": "^0.4.0",
    "passport-http": "^0.3.0",
    "uuid": "^3.3.2"
  }
}
